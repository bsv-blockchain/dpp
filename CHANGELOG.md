# Changelog

**Pre-1.0.** Everything below is the working draft assembling itself. Breaking changes are expected and are recorded here without apology until version 1.0 is declared, as `GOVERNANCE.md` states. Entries name the pull request that landed them; a change to a wire format names the fixture that moved with it, because the two are never allowed to drift apart.

## Unreleased

### 2026-09-06, prepared for independent implementation

- The adopter documentation is reconciled with the implementation. The root README names the current anchor format, the registry contract, the frozen industry profiles and their framework, the publisher key policy, static-peer synchronisation and the complete export as they exist, and states the two supported journeys (reference package consumer, independent implementer) and what each proves. `release/README.md`, `docs/quick-start.md` and `docs/migration.md` point at `dpp-release-2026-09-3`, the quick start gains the attestation-issuer and registry starts so every one of the six roles has one, the overlay README lists five extension routes, the profiles package names its runtime dependency, the core README says an independent implementation reproduces its rules rather than reusing them, and the Python reader's coverage statement matches its own limits. Package descriptions are current.
- `docs/` is the GitBook-compatible documentation root (`.gitbook.yaml`, `docs/README.md`, `docs/SUMMARY.md`): start here, learn the model, use the reference packages, implement independently, operate services, industry profiles, interoperability profiles, reference and contribute. The normative material stays canonical under `spec/`, `contracts/` and `fixtures/` and is linked by pinned repository URL, never copied. `scripts/docs-check.mjs` holds the navigation, every link and fragment, every pinned path, every example command and every version reference to the repository and the current set, and CI and `npm test` run it. No space is connected; the configuration is prepared so a connection changes nothing about the content.
- `release/dpp-release-2026-09-3.json` names the selection that qualifies it and declares, per entry point, the runtime, browser status, types, side effects, carried data and the Node built-ins each package imports; `release/release-set.schema.json` describes both. `conformance/check.mjs` holds the support declaration to the exports map and to a scan of each package's built code, holds the selection to the set, and validates `conformance/demonstrations/`. `docs/packages/support-table.md` and `docs/implement/requirements-matrix.md` are rendered from the set, the ledger and the fixtures by `scripts/render-support-table.mjs` and `scripts/render-requirements-matrix.mjs`, which refuse drift in check mode.
- The release pipeline binds one set to one revision. `scripts/release-candidates.mjs` refuses a set that is not a candidate, a manifest that does not export a declared entry point or a set without a selection, and records the set file's digest, the selection and the image tag beside the source revision. `scripts/lib/candidates.mjs` holds tarballs to the record (SHA-256, npm integrity, size, nothing missing, nothing unexpected) and the record to its set, `scripts/consumer-check.mjs` refuses to install until both hold and then imports every declared entry point including the `@bsv/vsc` subpaths, reads a sample of every data entry point, probes every carried artefact and type-checks all four packages and both subpaths with `skipLibCheck` off, and `conformance/test/release-scripts.test.mjs` exercises the refusals. `scripts/implementer-bundle.mjs` assembles the specifications, contracts, fixtures, conformance material, frozen profile data and implementer documentation with a digest manifest and no runtime code. `.github/workflows/publish.yml` is one job that selects the current candidate, derives the image tag from the set, runs every check and the selected-claim qualification and smokes the image before the first external write; `release/README.md` documents recovery from a partial publication.
- `docs/implement/` freezes the independent implementation contract: the bundle, the role order (reader and verifier under `native-baseline@2`, then writer and issuer, then separately operated registry and overlay), the permitted shared dependencies and the prohibited reference logic, the authorship and provenance record, six role guides with inputs, outputs, minimal path, expected results and refusals, the canonical bytes index, the service contracts and error vocabulary, the fixture runner, the reporting rules and the requirements-to-assertions matrix. `conformance/demonstrations/independent-implementation-2026-09.json` and `docs/implement/demonstration.md` define the interoperability and portability demonstration scenario by scenario with inputs, evidence, pass and refusal conditions and the rows and claims each bears on; nothing in it has run, and the ledger's statuses are unchanged.
- `docs/contribute/licence.md` states what the Open BSV License version 4 permits and conditions, what it applies to here, which artefacts carry third-party notices, and which reuse questions the maintainers have still to settle; no licence changes.

### 2026-09-06, the complete export

- `spec/portable-evidence.md` §2 gains the complete export, `dpp-evidence-export@1` (`contracts/evidence-export.schema.json`): parts over one snapshot of one source, each a `dpp-evidence-package@1` over a contiguous sequence range, oldest first, bounded by states and by transaction and proof bytes with at least one state per part, resumed by a cursor under the rules of §1, each carrying a coverage record (passport, snapshot, index, range, states, final flag, manifest digest) signed by the exporter and bound to the package manifest, tiling the snapshot so a reader joins them from the signed records alone and knows whether it holds everything the exporter asserted the snapshot held; a part is complete for its own range and never for the snapshot unless it is the only part, and carries no verification report. The reference index serves it at `GET /evidence-export` (`contracts/overlay.yaml` 0.7.0-draft; `@bsv/dpp-overlay-topics` 0.4.0), behind `EXPORT_TOKEN` when the deployment sets one, with `joinEvidenceExport` as the reader's join and the capability document stating the bounds and the access. The bounded `GET /evidence-package` is unchanged except that a package cut at the cap no longer names a genesis it does not hold. `test/evidenceExportParts.test.ts` exports 505 connected states as two parts, joins them, restores them into a second operator and exports them again; closes parts on either bound; keeps a state admitted mid-export out of the snapshot; refuses tampered, foreign, crossed and expired cursors by name; refuses a genuine first part dressed as a whole export by unsigned fields, every altered coverage fact, a substituted package and a record signed by another key; and names every join problem. `deploy/compose.yml` and `deploy/compose.second-operator.yml` forward `EXPORT_TOKEN`, and `deploy/operator.env.example` documents it. `release/dpp-release-2026-09-3.json` supersedes the second set before publication.

### 2026-09-06, selected-claim qualification

- `spec/conformance.md` §6 gains the selection and the qualification: `conformance/selection.schema.json` describes the claims a release or a declaration requires or withholds, each with its kind and its reason, and the exclusions a required system or product claim makes with a scope and a rationale; `conformance/qualify.mjs` reads a selection with the checker's own assessment (`conformance/assess.mjs`, shared with `check.mjs`) and exits non-zero while any required claim cannot be made, a required not-applicable row lacks its reasoning, an assessment rests on an unavailable source, or a row of a required full scope is neither required nor excluded. The checker keeps exiting zero on a blocked future claim and now validates every selection under `conformance/selections/`, reporting what each would refuse. Two selections are committed: `dpp-release-2026-09-3`, which is qualified, and `eu-dpp-system-2026-09`, which is refused until the six EN texts are read. `node --test conformance/test/*.test.mjs` runs the positive and refusal tests of the command and holds its verdicts equal to the checker's over the real ledger.
- The ledger records the public facts verified on 2026-09-06 about the European sources: Decision (EU) 2026/1736 of 14 July 2026 (OJ L, 2026/1736, 15.7.2026) cites the six EN 2026 editions for Articles 10 and 11 of the Ecodesign Regulation and presumes conformity for passports in conformity with them or parts thereof; EN 18239 and EN 18246 are drafts (prEN 2025) the decision does not cite, so their rows move to the `eu-dpp-system-draft` profile and a separate draft-compatibility claim. No normative EN text was read; every EN row stays unassessed or a gap and no clause is recorded.

### 2026-09-06, interoperability

- `spec/gs1-discovery.md` and the interoperability profile `gs1-digital-link@1` (`packages/dpp-profiles/manifests/interoperability/`, under the new `schemas/interoperability-profile.schema.json`): the full GS1 Digital Link grammar for primary key 01 with the consumer product variant, lot and serial qualifiers and data attributes, key-tuple equivalence that never joins a serial under another GTIN, EPC-binary decompression for SGTIN-96 and SGTIN-198 (the compression standard's own worked examples are vectors), resolution records with the resolver standard's hierarchy so a serial request answered by a model record is labelled model information, linkset production and selection validated against the pinned GS1 resolver schemas (`packages/dpp-profiles/schemas/gs1/`), and the resolver description file. The legacy evidence route on the API origin is kept and aliased at `/resolve/gs1/01/{gtin}/21/{serial}`. `fixtures/vectors/dpp/interoperability/gs1/` and `examples/resolve-digital-link.mjs`.
- `spec/epcis-interoperability.md` and the profiles `epcis-json@1` and `epcis-vsc@1`: `@bsv/vsc/epcis-source` reads an EPCIS 2.0.1 document as exact bytes under disclosed limits, refuses unsafe transports before retention, validates against the pinned schema (`packages/vsc/artifacts/epcis/`) without mutating a field, digests event bodies under RFC 8785 (`jcs.ts`), classifies arrivals as duplicate, new observation or conflict, labels events without an identifier as local, and builds the signed source reference `urn:bsv:dpp:epcis-source:1`; `mapEpcisEventWithReport` answers `lossless`, `transformed`, `unsupported` or `insufficient-data` with every gap named, and `mapEpcisEvent` is unchanged. `contracts/epcis-import.schema.json` is the durable import record and `contracts/interoperability.yaml` the application's routes for import, pull, review, publication and source export; no route performs a token operation. `fixtures/vectors/dpp/interoperability/epcis/` and `examples/import-epcis.mjs`.
- `spec/external-credential-profile.md` and the exchange profile `vc-di-ecdsa-rdfc-2019@1` (`spec/exchange.md` §1 gains the row): `@bsv/vsc/exchange` verifies a W3C VC 2.0 credential secured with `ecdsa-rdfc-2019` over a P-256 Multikey under a `did:web` issuer, with the maintained `@digitalbazaar/ecdsa-rdfc-2019-cryptosuite` 1.3.0 and `ecdsa-multikey` 1.8.0, reporting contexts, schema, proof, issuer binding, subject binding, time, Bitstring status, a named authority policy and availability separately; other curves, suites, inline contexts and multi-subject credentials are `unsupported`. The SEAL verifier is untouched. A reformatted credential's RDF proof still verifies while its exact-byte digest differs, and the vectors pin both. `fixtures/vectors/dpp/interoperability/external-credential/` and `examples/verify-external-credential.mjs`.
- `spec/passport-projections.md`, `contracts/passport-source.schema.json`, `contracts/passport-projection.schema.json` and `contracts/profile-evidence.schema.json`: append-only source revisions with a compare-and-swap current pointer, typed relationships (instance of model, member of batch, component of, successor of), the `passport-projection@1` envelope whose digest commits to pinned sources, policy, cutoff and disclosure and excludes its own identity, the precedence rules (declared overrides, conflicts with both sources, exact-scope measurements selected by observation time, corrections replayed to one result, zero and false preserved, availability apart from applicability, bounded traversal), and the shared claim evidence, certification, measurement, related document, language and date precision shapes. `@bsv/dpp-profiles` gains `projectPassport`, the manifest version 2 schema with its declarative applicability evaluator (`all`, `any`, jurisdiction, effective dates, thresholds and an `unresolved` outcome that is never a pass) and the generator's support for it. `fixtures/vectors/dpp/interoperability/projection/` and `examples/project-passport.mjs`.
- `battery@3` and `textile@3`, draft successors under manifest version 2, opt-in by explicit version: every field carries a requirement status (enacted, guidance, anticipated, needs-review, not-to-be-displayed or none) and a migration outcome from its predecessor; the battery successor applies the Commission's guidance of 15 August 2026 on data points by category, so the carbon, due diligence and recycled content fields are anticipated rather than required, industrial applicability that the guidance leaves conditional is unresolved rather than failed or passed, the state-of-health fields carry the guidance's category sets, and item-level dynamic companions exist for rated capacity, resistance, temperature and state of charge; the textile successor adds component-scoped composition, method-qualified assessments, certification evidence, related documents and language-tagged companions without mandating an unadopted score or scheme. `battery@2`, `textile@2` and every generated byte of theirs are unchanged.
- `@bsv/dpp-profiles` 0.3.0 (its first runtime dependency, `canonicalize` 4.0.0) and `@bsv/vsc` 0.2.0 (the `./exchange` and `./epcis-source` entry points); `release/dpp-release-2026-09-2.json` names the set and supersedes `dpp-release-2026-09` before publication, which the checker now keeps as history. Readiness records for EN 18223 serialisation and the Union registry are kept outside the repository; the capability statements say neither is implemented.

### 2026-09-06

- The capability document names `native-baseline@2` as the baseline the reference node claims, since the node admits both record versions; `conformance/examples/capabilities-reference-node.json` regenerated.
- `spec/record-model-v2.md`, record version 2: a seventeen-field layout selected by field count beside the fourteen-field version 1, with framed, domain-tagged signature preimages under `[1, 'dpp token v2']`, explicit `lineage_genesis` and `previous_outpoint` fields, a `control_linkage` proof on every non-genesis operation in the fixed order equality, authority, linkage, a terminal `RETIRE`, an `authorisation_commitment` a profile can require, four operations (`ISSUE`, `UPDATE`, `TRANSFER`, `RETIRE`) that `spec/rules.md` §2 maps to the version 1 vocabulary, and a single upgrade transition, a version 2 `UPDATE` keeping the controller key over a version 1 tip. Nothing changes for version 1 states. `fixtures/record-v2.json`, `fixtures/chain-v2.json` (five states, twenty-two refusals and the upgrade over the version 1 chain) and their vector forms move with it.
- `spec/managed-custody.md`, the profile `managed-custody@1`: a custodian holds the keys for parties without wallets, records and signs the recipient's acceptance as `dpp-managed-acceptance@1` (canonical JSON, custodian-signed, committed to by its SHA-256 in field 15 of the `TRANSFER`), and the binding of record to state is checked by a stranger from the two alone. The evidence is stated as custody-dependent, never as a recipient signature. `fixtures/managed-acceptance-v1.json` and `fixtures/vectors/dpp/managed-acceptance/v1.json` pin the record, signature, commitment, transfer and six refusals.
- `spec/verification.md`: the report is unchanged in shape and version; `linkage` gains the shared reason codes `lineage-retired`, `control-not-proven`, `version-transition-invalid` and `acceptance-commitment-absent`, `evidenceAvailability` covers acceptance records, `issuerAuthority` gains the role `acceptance-custodian`, forks are reported apart from rival geneses, and the limits name the version 1 preimage boundary, the custody-dependence of an acceptance commitment and a supplied fork. `fixtures/evidence-v2.json` pins thirteen version 2 cases; `fixtures/evidence-v1.json` and `fixtures/battery-lifecycle-v1.json` are regenerated with the per-state `version` and `controlValid` detail and the version 1 limits sentence, no finding changed.
- `@bsv/dpp-core` 0.3.0: `DppState` is the union of `DppStateV1` and `DppStateV2` (a consumer that switched on `op` alone now switches on `version` too), `parseDppOutput` reads both layouts, `checkTransition` takes a link context (predecessor index, lineage genesis, control authorities), `verifyChain` and `verifyPassportEvidence` take `controlAuthorities` and `managedAcceptance`, `checkControl` and the `acceptance.ts` module are new, and every version 1 export is unchanged.
- `@bsv/dpp-overlay-topics` 0.3.0: `tm_dpp` admits both versions, traces a version 2 state's lineage genesis through the submitted BEEF, its recent memory or the engine's storage and refuses a state whose genesis it cannot trace, refuses anything after a `RETIRE` and a genesis that spends an admitted output, and takes `ACCEPTANCE_COMMITMENT=required` (the managed-custody profile) and `CONTROL_AUTHORITIES`; the capability document names protocol `dpp-record` version `2` and a custody profile entry (`managed-custody@1` or `record-model-baseline@2`); `contracts/overlay.yaml` 0.6.0-draft describes the admission. `@bsv/dpp-profiles` 0.2.0 maps the four version 2 operations through the version 1 mapping and knows `native-baseline@2`.
- The stack moves to `@bsv/sdk` 2.4.2, `@bsv/overlay` 2.3.1 (`@bsv/gasp` 1.3.6) and Node 22 in every package, the CI workflow and the index image. Two behaviours of the new stack are recorded because a writer or operator meets them: `Beef.toBinaryAtomic` now serialises exactly the subject and its unproven ancestry, so a proven predecessor merged beside a proven subject is no longer in the atomic bytes and the index judges such a state against the predecessor it holds (the storage fallback it has had since the backfill defect); and a state a peer synchronises arrives as an atomic BEEF and is stored in that form, which decodes to the same transaction. The engine storage adapters take the upstream `deleteAppliedTransaction(txid, topic)` signature, which the Engine now declares itself; the Engine's unproven-admission eviction, block anchors and reorganisation handling stay off, because the storage does not implement the optional members they need, and the capability document does not claim them.
- `deploy/`: the supported operator preset on Docker Compose (the image, a MongoDB, named volumes, one env file) with a second-operator overlay and the recipe for start, restart, backup, restore from an evidence package and synchronisation; `docs/deployment.md` records the rehearsal and the evaluation of `@bsv/overlay-express` 2.6.1 as the shared host, which was not adopted (the DPP contract routes would remain an adapter, its SQL engine storage wrote behind the answer and split a lineage in the evaluation, and its maintenance evicts unproven history). `POST /requestForeignGASPNode` answers from the node's own storage by the transaction a peer names, so a fresh operator synchronises a lineage whose tip is proven, which the engine's own walk cannot serve since it stores proven states compactly.
- `release/`: the compatible release set `dpp-release-2026-09` (packages and versions, wire versions, the selected custody profile, the runtime, artefact digests, the consumers) under `release-set.schema.json`, held to the repository by `conformance/check.mjs` and re-pinned by `conformance/pin-sources.mjs`; `scripts/release-candidates.mjs` packs the four packages into candidates with recorded digests, `scripts/consumer-check.mjs` installs exactly those into a clean project outside the checkout and checks every entry point, the fixtures, the carried artefacts and the declarations; CI gains the consumer job and an image smoke, and `.github/workflows/publish.yml` publishes a named set by hand, dry-run by default. `@bsv/dpp-core` 0.3.0, `@bsv/dpp-overlay-topics` 0.3.0, `@bsv/dpp-profiles` 0.2.0 and `@bsv/vsc` 0.1.1 (unchanged code; its manifest now requires Node 22) are the set's candidates.
- `conformance/baseline-native-2.json` (`native-baseline@2`), the ledger rows for both new documents, and `conformance/check.mjs` validating the second baseline and the version 2 reports. `conformance/independent/python/dpp_verify.py` reads both layouts, both preimage formats, the control proof, retirement, the upgrade and the acceptance record, and reproduces every version 2 fixture and vector. `examples/verify-passport.mjs --version=2` replays the version 2 fixtures and `examples/lifecycle-v2.mjs` builds a managed-custody lifecycle from fresh keys; CI runs both.

### 2026-09-05

- `spec/verification.md`, the one verification contract: a report of sixteen named checks with four answers each, an independently expected subject, an observation of the latest state that is never a proof, and stated limits, with `contracts/verification-report.schema.json` as its schema. `@bsv/dpp-core` gains `verifyPassportEvidence` and `inspectChain`, the loop `verifyChain` has always run reported finding by finding; `verifyChain` and its result are unchanged. `fixtures/evidence-v1.json` pins the report for twenty-one cases built from the chain and anchor fixtures, and `examples/verify-passport.mjs --report` is the standalone surface that reproduces every one, which CI runs.
- The generic anchor `bsv-attestation-anchor-v1` moves into `@bsv/dpp-core` as `anchor.ts`, with `inspectAttestationAnchor` reporting layout, key derivation and signature separately; `@bsv/dpp-overlay-topics` re-exports every name unchanged, so no consumer edits an import.
- `spec/profiles.md` and `@bsv/dpp-profiles` (`packages/dpp-profiles`): the canonical industry data profiles as immutable manifests under a JSON Schema 2020-12 manifest schema, inventoried in full from the consuming application's registries (105 battery, 49 textile, 40 general fields, plus the superseded textile@1 and general@1) and frozen by digest; a deterministic generator producing the public and restricted payload schemas and the legacy consumer documents, which reproduce the application's published profile documents byte for byte; obligation separate from legal basis, `needs-review` applicability that is never read as satisfied, and the model, batch and item identity rules; GS1 check-digit, GTIN-14, demonstration-prefix and Digital Link helpers for writers; and `checkSelection`, which passes the supported combinations of baseline, industry, exchange and operator profiles and refuses contradictory selections by name.
- `spec/exchange.md`: credential representations beside the native record as named, versioned exchange profiles; the four artefacts kept apart (native claim, original secured bytes, decoded value, projection); signer rules with ES256K as a supported suite a consumer confirms and an optional, separately keyed ES256; the native evidence extension (`contracts/native-evidence-extension.schema.json`, `urn:bsv:dpp:native-evidence:1`) by which an external credential references existing native evidence without a circular digest; and exact-byte anchoring under the generic anchor. `@bsv/dpp-profiles` catalogues exchange profiles (`untp-0.7.0-jose@1` proposed with every UNTP artefact recorded as not yet retrieved, `vsc-draft-compat@0.1.0` current with the owned artefacts' digests) and operator profiles (`single-operator@1`, `federated-operators@1` proposed) under their own schemas.
- `spec/portable-evidence.md`: an export answers in pages over a stable snapshot that say what they are (`contracts/paginated-history.schema.json`), and evidence leaves an operator as `dpp-evidence-package@1`, a signed manifest over a content-addressed inventory whose signature authenticates the exporter and the inventory and nothing more (`contracts/evidence-package.schema.json`). `spec/rules.md` §2 states the four-valued mapping result and `spec/services.md` §3 the claim relations that no token spend implies. `@bsv/dpp-profiles` generates a row-level field mapping inventory for each current profile and implements the mapping as `mapNativeOperation`: structured evidence facets per operation, the four outcomes, and a loss list on every outcome that is not lossless.
- `contracts/registry.yaml` follows the registry's interface: `POST /validate` answers under a `contract` discriminator (`attestation-registry/1` or `legacy-uora`) and carries the verification report, `GET /attestations/{id}/report` reports a stored record, the status list routes (`GET /status/{listId}` verbatim bytes; create, allocate, revoke, suspend and clear behind the write token) publish signed lists, and `x-intentional-omissions` names the two served routes the contract leaves out with their reasons; the registry holds the file identical to its own and checks it against the served routes by machine. The registry's Batch 2 adds `claimRelations` (supersedes, revokes, suspends, expires, with conflicts returned unresolved across frameworks) and `lifecycleMapping` to its record, report, history and validate answers, and serves `/spec/profiles` from `@bsv/dpp-profiles`. Its Batch 3 adds `GET /attestations` and paged `/history` and `/chain` answers over a fixed snapshot with opaque cursors, the evidence package export `GET /passports/{passportId}/evidence-package`, and a `ChainRecord` documented field by field, with anchors carrying `format`, `lockingScript` and `broadcastAt` where retained.
- `fixtures/vectors/dpp/publisher-policy/v1.json` and `fixtures/vectors/dpp/evidence-package/v1.json`: portable vectors for the two new contracts in the stack's format, generated by core tests and held identical; the Python reader reproduces every preimage, digest, signature and inventory verdict and reaches the same refusals.
- `fixtures/battery-lifecycle-v1.json`: the marked industrial-battery demonstration lifecycle, generated by `@bsv/dpp-profiles` tests from the core's fixture helpers: a synthetic passport under the GS1 demonstration prefix 952 through manufacture, sale with movement evidence, a token transfer with no custody evidence, repair, reassessment and recycling into new identities, each state with its signed native claim, evidence facets and mapping result, under one verification report.
- `docs/quick-start.md`: one complete start per role (reader, verifier, writer, profile author, operator), the named-result rule on a capability mismatch, an unknown extension or a version downgrade, the custody and account pointers, and every conformance command in one place; each command is one CI runs.
- The reference index node (`contracts/overlay.yaml` 0.5.0-draft, existing routes unchanged): `GET /capabilities` serves the capability document built from constants and configuration; `PUBLISHER_POLICY_FILE` and `OPERATOR_IDENTITY_KEYS` make `tm_dpp` admit a state only under a publisher key active at the state's own timestamp and `tm_attestation` an anchor only under a key active at admission, refusing to start on a chain that does not hold; `GET /history` answers pages over a snapshot fixed at the source's sequence with tamper-checked cursors; `GET /evidence-package` exports a signed `dpp-evidence-package@1` (with `EXPORT_SIGNING_KEY`) and a second operator restores from it; `POST /retract` lets a writer withdraw an admitted output the network refused, guarded by the submit bearer, the held merkle path and the network check. Records carry an insertion sequence on both stores. The Dockerfile copies the profiles manifest the lockfile now knows. The node also serves the upstream GASP routes and synchronises both rails from declared peers (`SYNC_PEERS`, `SYNC_INTERVAL_MS`, `SYNC_LEGACY`) under the same admission rules; a refused spend of a tip no longer erases its lineage (`tm_dpp` retains the offered coins and `ls_dpp` records a spend only on admission); the export is capped at 500 states with the remainder declared absent; a synchronisation answer is bounded to 500 outputs; a transaction the index refused earlier is re-evaluated when announced again instead of reading as a duplicate; and the capability document names an unrestricted anchor rail as unsupported.
- `contracts/publisher-policy.schema.json` (`dpp-publisher-policy@1`): the bounded, versioned set of publisher keys with activation and retirement times, a superseded-version digest and an authorisation the previous version accepts, so a state is checked against the key active at its time and a handover never edits a list (`spec/services.md` §1). `spec/profiles.md` §3 states that a field's value kind (fact, declaration, calculation, assessment) is never upgraded by a signature and that accreditation is a separately confirmed authority.
- `conformance/independent/python/dpp_verify.py`: a second reader in standard-library Python that shares no code with the reference, reproducing every pinned preimage, derived key, digest and script and refusing every refusal vector across the four fixtures and the vector files; CI runs it. Written within this programme, so it is engineering evidence and not the independent implementation GOVERNANCE.md requires before 1.0, which the ledger records.
- `spec/conformance.md`, `conformance/manifest.json`, `conformance/baseline-native-1.json` and `contracts/capabilities.schema.json`: the requirement layers, the role matrix, the recommended `native-baseline@1`, the capability document, the identity vocabulary and the source-pinning rules, with a checker that validates the ledger and blocks any claim whose required rows are unassessed or whose sources have moved.
- Add the VSC draft compatibility profile, owned context/schema artefacts, genuine Ed25519Signature2020 and BBS proofs, explicit status/authority verification and EPCIS mapping. Separate product/actor identity from application accounts and native BSV custody.
- `@bsv/dpp-core` and `@bsv/dpp-overlay-topics` move from 0.1.0 to 0.2.0: `DppRecordStore` gains required members and `DppRecord` a required sequence, which an external store implementation must add, and the overlay contract's `anchorLookup` component now names the `ls_attestation` answer while the historical one is `legacyAnchorLookup` (identical wire; a client generated from component names sees the rename).
- Add bsv-attestation-anchor-v1, tm_attestation and ls_attestation. The signed ten-field anchor declares the representation and commits to its complete secured bytes. Add exact outpoint cursor pagination and portable cross-implementation fixtures.
- Native lifecycle claims now sign claimFormat and eventType; the new anchor digest includes the native signature. Preserve historical UORA-named bytes and APIs under explicit legacy rules. Publish the registry interface and migration boundaries.

- `fixtures/vectors/dpp/record/v1.json` and `anchor/v3.json` are generated by tests, as the chain's has been since #32: `packages/dpp-core/test/record-v1-vectors.ts` and `packages/overlay-topics/test/anchor-v3-vectors.ts` map the bespoke fixtures to the stack's vector format, the anchor generator building the four refusals the bespoke file lacks from the pinned output, and each package's fixture-consistency test holds the published file identical to its generator's output. `REGENERATE_FIXTURES=1` rewrites every published fixture from its generator, so none is edited by hand. The anchor file's `reference_impl` names the package that exists, `dpp-overlay-topics`, in place of `@dpp/overlay-topics`; no vector changed (#35).
- `spec/record-model.md` §3: the trade item number inside a GS1 Digital Link `passport_id` is allocated, never chosen. A writer publishes only under a GS1 Company Prefix licensed to the brand the record describes; a record that describes no real object carries a GTIN under GS1 prefix 952, the prefix GS1 reserves for demonstrations and never licenses; the check digit is correct when written and not a reader's to verify; the host is the writer's choice and should be one that answers for the identifier. `spec/design-rationale.md` records why, with the demonstration's live example number as the evidence. `docs/identifiers.md`, informative, is the account of how a brand obtains a prefix, what each host answers, what GS1 Switzerland charges, and how an implementation holds the number so a licensed GTIN replaces a demonstration one by changing a setting. The fixtures still carry `09506000134352` and move to a 952 number in a follow-up, which regenerates every published file (#34).

### 2026-08-30

- `spec/writing.md`, the seventh normative document (#33): what a writer owes the record, as outcomes. The verifier's own check before anything is sent; announce before sending; one writer per passport and no state reported as written before the network answers; broadcast through the wallet and through more than one door; a failed announcement never fails a record and is retried rather than rebuilt; the merkle path obtained, kept and offered to the index; the bytes retained for the passport's life. A personal wallet discharges the broadcast, proof and retention duties itself. `services.md` gains the index's duty to serve mined states with their proofs and the ways it can learn of mining (re-announcing is not one, because admission of a held output is a no-op); `record-model.md` §8 makes the header source a configuration and says the proof is obtained once and travels with the record; the rationale and governance (writer conformance is self-reported, one sentence per rule) cross-reference the new document.
- `contracts/overlay.yaml` 0.3.0-draft: `POST /arc-ingest` moves from may to should, with the proof checked to contain the transaction and validated against the header source before it is stored, and is documented as a path with its answers (#33).
- `@bsv/dpp-overlay-topics` serves `POST /arc-ingest` in the shape an ARC-compatible callback carries; the lookup then answers the state with its merkle path. `ARC_CALLBACK_TOKEN` guards it as a bearer or `X-Callback-Token` (#33).
- `examples/write-passport.mjs`: the writer's lifecycle from one BRC-100 wallet under possession, with a dry run CI runs that rebuilds the fixture's first state byte for byte and shows the writer's own check refusing a forbidden state before it could be sent (#33).
- `docs/stack.md` gains the writing section and the proof-ingestion follow-ups; `docs/deployment.md` names the deployment that follows the standard's defaults, the one place the ecosystem's endpoints appear (#33).
- `spec/custody.md`, the sixth normative document (#32): the four keys a state involves and their roles, the constraint that every role is fillable by a BRC-100 wallet with one derivation and no key export, the pinned convention that a lock held by the owner is the field-6 key, the optional owner-signed transfer a profile may select (equality or `owner_linkage`, with transfer authorities for recovery), and the three custody arrangements with their costs, stated as peers. `record-model.md` gains the field-6 derivation as a SHOULD, reserves `owner_linkage` on `TRANSFER` and names the optional eighth invariant; the rationale records the single-key lock as a decision; identity, services and the overlay profile cross-reference the new document.
- `@bsv/dpp-core` gains `owner.ts` (the owner key, the linkage scalar, its BRC-100 revelation and decryption, the pure linkage check and the consent predicate), `verifyChain`'s `ownerConsent` option and `StateCheck.ownerConsentValid`, a required property of every result and so a pre-1.0 type change (#32).
- `fixtures/chain-v1.json` grows two states, the first locked to the owner's per-passport key and the second spent from the owner's wallet with `owner_linkage` in `event_data`, and seven consent refusals that are valid chains with the option off; `fixtures/vectors/dpp/chain/v1.json` is the chain's vector form, the first generated by a test and held identical (#32).
- `@bsv/dpp-overlay-topics`: `tm_dpp` takes the owner-signed transfer as an admission option, configured by `OWNER_CONSENT` and `TRANSFER_AUTHORITIES`, and documents per instance whether it enforces it; `examples/verify-passport.mjs` takes `--owner-consent` and CI runs that form (#32).
- The standard is aligned with the BSV TypeScript stack, one commit per component (#31). `spec/record-model.md` names the output a BRC-48 PushDrop and says why a generic decoder is not a conforming reader, spells out the BRC-42/43 invoice numbers, bounds `passport_id` at 512 bytes and `actor_keyID` at 256 because both are BRC-42 key identifiers, and recommends UHRP hosting and AES-256-GCM under a wallet-derived key for the owner tier. `@bsv/dpp-core` enforces the bounds and `fixtures/record-v1.json` gains the `overlongPassportId` refusal vector.
- `spec/rules.md` names the anchor a BRC-48 PushDrop, spells out both invoice numbers, bounds the claim's `passportId` at 512 and defines the preimage's VarInt; `spec/identity.md` cites `@bsv/did` and the canonical-key check it lacks, and names `did:bsv` and `did:web` (#31).
- `contracts/overlay.yaml` becomes a profile of ts-stack's overlay HTTP contract with an `x-dpp-profile` block of must, should and may; `spec/services.md` adds SHIP/SLAP discoverability, overlay-first announcement and idempotent admission (#31).
- `fixtures/vectors/`: the same bytes in the stack's cross-language conformance vector format, 25 vectors validated by the stack's runner, with the test private keys published and four anchor refusals the bespoke file lacked (#31).
- `docs/stack.md`: the map from every rule to the stack component behind it, and the follow-ups that belong in other repositories; `GOVERNANCE.md` rule 3 counts both fixture forms (#31).

### 2026-08-26

- `@bsv/dpp-core` gains `canonicalBytes` and `canonicalString`, the refusing JCS subset of rules.md §4, previously held only by the implementing services. Pinned to `fixtures/anchor-v3.json`'s canonical string and digest, and the anchor example now canonicalises the claim itself.
- `examples/verify-anchor.mjs`: the anchor rail's verification recipe as runnable code, against `fixtures/anchor-v3.json`, every check of rules.md §6 and every refusal vector; CI runs it on every change.
- Security-only Dependabot configuration; vulnerability alerts and automated security fixes switched on (#27).
- `GOVERNANCE.md` second draft merged (#22); `rules.md` §5 records the anchor reader's leniencies as removed (#25).
- `examples/verify-passport.mjs`: the standard's own verification recipe as runnable code, against any index or offline against the chain fixture; CI runs the offline form on every change.
- Both packages declare `publishConfig.access: public` (#24).
- `fixtures/chain-v1.json`: the chain invariants join the conformance seed. A four-state pinned chain with the DPP output moving to index 1 mid-chain, and nine refusal vectors, one per broken invariant, each readable from raw transaction hex alone.
- `CHANGELOG.md` begins, seeded from the first commit, and the fixtures README links each file's leading copy (#21).
- `GOVERNANCE.md` second draft: roles, acceptance thresholds, the conditions for 1.0, security reporting (#22, in review).
- The README describes the repository as it is: present tense, the five spec documents named, both fixtures landed, CI stated (#19).
- The design rationale states reasons for the two rails staying separate and claims nothing about how the decision was reached (#20).

### 2026-08-24

- The reference implementation moves in: `packages/dpp-core` and `packages/overlay-topics` under the public names `@bsv/dpp-core` and `@bsv/dpp-overlay-topics`, the Dockerfile that builds the index node from this repository alone, CI running both suites and the image, and two tests holding `fixtures/*.json` byte-identical to the modules that generate them. Every reference to a private document was remapped to `spec/` and `contracts/` (#17).
- `spec/design-rationale.md`: why the settled choices are what they are, hung off the one property the standard protects (#18).

### 2026-08-23

- `contracts/overlay.yaml` matches the served wire: `X-Topics` optional and defaulting to `tm_dpp`, the `X-Includes-Off-Chain-Values` header, the 413 refusal at 8 MiB, `/health`'s singular probe fields, and `coinsRemoved` present only when the mutation phase ran (#16).

### 2026-08-22

- `spec/record-model.md` states its refusals exactly: non-minimal pushes accepted for non-empty fields only, `OP_0` the sole encoding of the empty field, the single byte `0x00` refused, UTF-8 fields must round-trip, rolled-over dates and the legacy `24:00` refused, and `failed` covers a proof that refutes itself (#14).
- `fixtures/record-v1.json`: the record rail joins the conformance seed, positive vectors and seven refusal vectors (#15).

### 2026-08-21

- The shape of the standard: README, `GOVERNANCE.md`, Open BSV License version 4.
- `spec/record-model.md` (#1), `spec/rules.md` (#2), `spec/identity.md` and `spec/services.md` (#8); corrections from adversarial verification (#11).
- `contracts/overlay.yaml`, the index as OpenAPI (#3); lookup selector floors from live verification (#10).
- `fixtures/anchor-v3.json`, the anchor byte for byte (#4); refusal vectors for the reader leniencies (#13).
- README names the first consuming application (#9).
