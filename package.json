{
  "name": "dpp",
  "private": true,
  "description": "Digital Product Passport standard: specification, reference implementation, conformance fixtures",
  "license": "SEE LICENSE IN LICENSE",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @bsv/dpp-core -w @bsv/dpp-overlay-topics -w @bsv/vsc -w @bsv/dpp-profiles",
    "test": "npm run test --workspaces --if-present && npm run conformance:check",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "conformance:check": "node conformance/check.mjs",
    "conformance:pin": "node conformance/pin-sources.mjs",
    "conformance:independent": "python3 conformance/independent/python/dpp_verify.py"
  },
  "overrides": {
    "@bsv/overlay": {
      "@bsv/sdk": "2.1.4"
    }
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1"
  }
}
